/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // Note: API routes require server-side rendering
  // For Vercel: Use default config (API routes work)
  // For GitHub Pages: You'll need a separate backend (FastAPI) or use Vercel
}

module.exports = nextConfig

